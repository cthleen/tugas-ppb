# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# Increased memory and ADB timeout properties
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8 -Dcom.android.ddmlib.adb_extension_timeout=300000

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects.
# org.gradle.parallel=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
android.disallowKotlinSourceSets=false

# Increase ADB timeout to prevent TimeoutException during install
# These properties cover different ways the timeout can be specified
systemProp.com.android.ddmlib.adb_extension_timeout=300000
android.adb.timeout=300000
android.injected.install.timeout=300000
